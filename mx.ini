[settings]
# Install the local package in editable mode with dev extras
requirements-in = requirements.txt
default-install-mode = editable
default-target =
# Target executed by `make run`. run-capture starts the capture/planner
# app with the lean capture install profile (see include.mk).
run-target = run-capture
