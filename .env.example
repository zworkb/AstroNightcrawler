# Sequence Planner — Configuration
# Copy to .env and adjust as needed. All values have sensible defaults.

# Web server
SEQ_HOST=0.0.0.0
SEQ_PORT=8090

# Capture output directory
SEQ_OUTPUT_DIR=./output

# INDI server (telescope/camera control)
SEQ_INDI_HOST=localhost
SEQ_INDI_PORT=7624
